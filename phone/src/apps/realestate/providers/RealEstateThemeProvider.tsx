import { createAppThemeProvider } from '@os/apps/utils/createAppThemeProvider';

import realEstateTheme from '../realestate.theme';

export const RealEstateThemeProvider = createAppThemeProvider(realEstateTheme);
